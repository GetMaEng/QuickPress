# Local dev: talk straight to the Hono server started by `npm run dev` in Backend/.
# Not used in production - the Vercel rewrite in vercel.json handles /api there.
VITE_API_URL=http://localhost:3000
